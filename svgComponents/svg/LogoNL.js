import React from "react";
import Svg, {  } from "react-native-svg";

const LogoNL = props => (
<Svg width={props.width || 1023} height={props.height || 275} viewBox="0 0 1023 275"></Svg>
);

export default LogoNL;
